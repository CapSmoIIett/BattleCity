// Для основы логики игры был выбран С++, из за острой необходимости в классах
#include <GL/gl.h>

int main (int argc, char** argv)
{
    // При запуске через консоль можно ввести параметры включающие режим отладки
    MainMenu(argc, argv);
    return 0;
}